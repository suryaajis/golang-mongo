interface:
  display_name: "Golang Mongo"
  short_description: "Repo-specific patterns and workflows for golang-mongo"
  default_prompt: "Use the golang-mongo repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true